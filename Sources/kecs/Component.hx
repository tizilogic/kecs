package kecs;


/**
 * The `Component` is simply an empty interface to leave the implementation up
 * to the needs of the user. A `Component` usually is a container holding data,
 * which is updated/modified by a `kecs.System`.
 */
interface Component {}
